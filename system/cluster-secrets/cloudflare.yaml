apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: cloudflare-api-token-secret
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: k8s-configuration
    namespace: doppler-operator-system
  project: k8s-configuration
  config: prd
  managedSecret:
    name: cloudflare-api-token-secret
    namespace: cert-manager
    type: Opaque
